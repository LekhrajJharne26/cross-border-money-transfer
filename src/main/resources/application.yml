spring:
  application:
    name: cross-border-money-transfer

  datasource:
    url: jdbc:mysql://localhost:3306/cross_border_db
    username: root
    password: ############
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

  jackson:
    default-property-inclusion: non_null

server:
  port: 8080
  servlet:
    context-path: /api

application:
  security:
    jwt:
      secret: #############################
      expiration-ms: 900000

springdoc:
  swagger-ui:
    path: /swagger-ui.html
